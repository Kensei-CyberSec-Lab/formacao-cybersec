# Environment Configuration for Kali Linux Containers

# Base image (override to pin a specific Kali build if needed)
KALI_IMAGE=kalilinux/kali-last-release:latest

# VNC Configuration
VNC_PASSWORD=kalilinux
VNC_GEOMETRY=1920x1080
VNC_DEPTH=24

# Display Configuration (for X11 forwarding)
DISPLAY=${DISPLAY:-:0}

# Container Configuration
TIMEZONE=UTC

# Network Configuration
PENTEST_SUBNET=172.20.0.0/24
PENTEST_GATEWAY=172.20.0.1
