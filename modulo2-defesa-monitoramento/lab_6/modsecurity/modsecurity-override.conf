# This override file is included by the image-generated /etc/nginx/modsecurity.d/modsecurity.conf
# Start in DetectionOnly; switch to 'On' during containment phase of the lab.
SecRuleEngine Off

# Log tuning (kept compatible with image defaults)
SecAuditEngine RelevantOnly
SecDebugLogLevel 0
